# Copiar este archivo a `.env.local` y ajustar valores en tu máquina.
# Los archivos `.env.local` están en .gitignore y no deben subirse a GitHub.
#
# Ejemplo para Next.js (añadir claves reales solo en local):
# NEXT_PUBLIC_APP_NAME=Sistema Integral Concretos Tepexi
